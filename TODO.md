# Bili-Mux 功能路线图

按梯队排优先级。所有方案均基于当前架构：content.js 拉流 → 分块 base64 → offscreen → ffmpeg.wasm → 下载。

状态图例：`[ ]` 待做　`[~]` 进行中　`[x]` 已完成

---

## 0. 通用技术约束（每个功能都要遵守）

这几条是从现有代码里踩过的坑，新功能一律适用：

| 约束 | 说明 |
|---|---|
| **必须加 `-fflags +genpts`** | B 站 fMP4 流存在非单调 DTS，不加会导致 muxer 丢包，输出只剩输入的 1/6 左右 |
| **`run()` 会抛 ExitStatus** | 单线程 core 在 `exit(0)` 时从 Promise reject 抛出，但 ffmpeg 实际已成功。必须 catch 后检查输出文件是否存在，不能当成失败 |
| **输出体积校验** | 输出 < 输入 90% 判定为丢包异常（现有 mux 已有此逻辑，新功能照抄） |
| **MEMFS 用完 `unlink`** | 否则多次操作后内存堆积直至 OOM |
| **进程间传二进制必须 base64** | `chrome.runtime.sendMessage` 只支持 JSON，ArrayBuffer 会序列化成 `{}`。单条消息上限 64MiB，现有 `RAW_CHUNK = 16MB` |
| **先分清「容器操作」还是「重编码」** | 单线程 core 重编码约 0.1–0.5x 实时。`-c copy` 类秒级可随便做；重编码类必须加时长/分辨率闸门 |
| **输入命名约定** | 视频 `inv.{ext}`、音频 `ina.mp4`（fMP4 封装的 AAC）、FLV `in.flv`、输出 `out.{ext}` |
| **不轻易加 `-movflags +faststart`** | 从 fMP4 复制时可能失败导致静默无输出 |

---

## 1. 元数据擦除是什么（先解释这个）

**元数据 = 文件里除了音视频数据以外的描述信息。** 常见内容：

- 容器级：`title`、`artist`、`comment`、`creation_time`、`encoder`、MP4 的 `©too` 标签
- **位置信息**：手机拍摄的视频/MOV 常带 GPS 坐标（`©xyz` 标签）
- 设备信息：拍摄机型、镜头参数、软件版本
- 流级：编码器名称与编码参数

擦除命令（`-c copy` 秒级，不重编码）：

```
-i in.mp4 -map_metadata -1 -fflags +bitexact -flags +bitexact -c copy -y out.mp4
```

- `-map_metadata -1`：清除全部容器级元数据
- `-fflags +bitexact` / `-flags +bitexact`：不让 ffmpeg 写入自己的编码器标识（`Lavf60.x`）。不加的话，处理后的文件等于被打上「我用 ffmpeg 处理过」的记号

### 但要说实话：对 B 站下载场景，这个功能是伪需求

B 站的 m4s 流本来就没携带什么元数据，更不可能有 GPS。清了也就是省几 KB，用户感知接近于零。

**真正有价值的场景是「本地文件处理台」**——用户把自己手机拍的视频拖进来处理时，GPS 坐标是实打实的隐私泄露，这时候擦除才有意义。

**所以建议不要单独做按钮**（在 B 站场景下点了也白点），改为：

1. 在「合成 MP4 / 保存视频 / 保存音频」时默认勾选「清除元数据」——成本近乎为零，顺手就做了
2. 等将来做本地文件处理台时，再把它提升为主打功能

---

## 2. 第一梯队

### 2.1 无损片段裁剪　`[ ]`　工作量：中

**目标**：选起止时间，切出一段，秒级完成。

**命令**：

```
-fflags +genpts -ss {start} -i inv.m4s
-fflags +genpts -ss {start} -i ina.mp4
-t {duration} -map 0:v:0 -map 1:a:0 -c copy -avoid_negative_ts make_zero -y out.mp4
```

两个输入各自 seek，保证音视频同步。`-avoid_negative_ts make_zero` 修正 copy 裁剪后的负时间戳偏移。

**必须知道的精度问题**：

| 模式 | 命令 | 速度 | 精度 |
|---|---|---|---|
| 快速（默认） | `-ss` 放在 `-i` **前** | 秒级 | 只能定位到关键帧，可能偏差数秒（B 站关键帧间隔约 2–10s） |
| 精确（可选） | `-ss` 放在 `-i` **后** | 需重编码，慢 | 精确到帧 |

`-c copy` 不解码，因此只能用快速模式。UI 上要注明「精确到最近关键帧」，并提供「精确模式（需重编码）」开关。

**UI**：面板加裁剪区，起止时间输入框 + 「裁剪并保存」。可选：解析时长后自动填入默认值。

**风险**：copy 裁剪后开头可能有短暂黑屏或无声（关键帧前的数据缺失），属已知现象，可接受。

**依赖**：需要已 mux 的成品或双流输入；建议复用现有 mux 通道。

---

### 2.2 提取即转码：音频 → MP3，视频 → MP4　`[ ]`　工作量：小

**目标**：现在「分离保存」存的是 `.m4s`，用户双击打不开。改成直接出成品。

**音频 → MP3**：

```
-fflags +genpts -i ina.mp4 -vn -c:a libmp3lame -b:a 192k -y out.mp3
```

**这是重编码**（AAC → MP3）。音频重编码约 10–30x 实时，1 小时音频约 1–3 分钟，可接受。

但要注意：**AAC → MP3 是有损到有损的二次编码，音质必然有代损。** 所以同时提供 M4A 选项：

```
-fflags +genpts -i ina.mp4 -vn -c:a copy -y out.m4a
```

`-c copy` 秒级且零音质损失。**默认给 MP3**（你要的），但下拉里保留 M4A，并在文案里说清代价。码率选项 128k / 192k / 256k / 320k，默认 192k。

**视频 → MP4**：

```
-fflags +genpts -i inv.m4s -c copy -y out.mp4
```

纯换容器，秒级。**但输出没有音轨**（B 站 DASH 视频流本身不含音频）。

UI 上必须写清楚：这个按钮出的是「无音轨 MP4」，想要带声音的请点现有的「合成 MP4」。

**改动**：`btn-video` / `btn-audio` 的输出扩展名与文案调整；文件命名从 `.m4s` 改为 `.mp4` / `.mp3` / `.m4a`。

---

### 2.3 元数据擦除　`[ ]`　工作量：小

见第 1 节。落地形式：**在保存/合成流程中作为默认勾选的附加项**，不单独占按钮位。

---

## 3. 第二梯队

### 3.1 弹幕烧录成硬字幕（开关）　`[ ]`　工作量：大　⚠ 有前置阻塞

**目标**：合成 MP4 时可选开关，勾上就把弹幕烧进画面。

**数据流**：

1. **取弹幕**：`https://api.bilibili.com/x/v1/dm/list.so?oid={cid}`，返回 XML，`oid` 即 cid
2. **XML → ASS**：B 站弹幕的 `p` 属性为逗号分隔：

   | 位置 | 含义 |
   |---|---|
   | 0 | 出现时间（秒，浮点） |
   | 1 | 模式：1 滚动 / 4 底部 / 5 顶部 / 6 逆向滚动 / 7 高级 / 8 代码 |
   | 2 | 字号 |
   | 3 | 颜色（十进制 RGB） |
   | 4 | 发送时间戳 |
   | 5 | 弹幕池 |
   | 6 | 用户 hash |
   | 7 | 弹幕 dbid |

   - 滚动弹幕（1/6）→ ASS 的 `\move(x1,y1,x2,y2,t1,t2)`
   - 顶部/底部（5/4）→ `\an8` / `\an2` 固定位置，持续 N 秒
   - **颜色转换注意**：ASS 用 `&HBBGGRR&`，是 **BGR 顺序**，和 RGB 反着来

3. **ASS → 烧录**：`subtitles=dm.ass` 滤镜

**⚠ 前置阻塞：字体**

已 grep 确认：`ffmpeg-core.wasm` 里 `fontconfig` 存在（libass 链接了），但 **`fonts.conf`、`.ttf`、`.otf`、`DejaVu` 全部为零**——core 里一个字体都没有。

直接 `subtitles=x.ass` 渲染中文会找不到字体，静默失败。

要解开这个阻塞，必须：

1. 用 fonttools 把思源黑体 / 文泉驿按 3500 常用字子集化，得到 1–3MB 的 TTF（相对现有 24MB core 增量可接受）
2. 运行时写入 MEMFS，并手写 `fonts.conf` 指向字体目录
3. 指定 `FONTCONFIG_FILE` 让 libass 找到配置

**开工前必须先做最小验证**：塞字体 + 一条测试 ASS 进 offscreen 跑一次，确认 libass 在 wasm 环境里真能读到自定义 fonts.conf。**这步不通则整个方案要改道**（备选：Canvas 用系统字体渲染弹幕为 PNG + `overlay` 滤镜，零字体依赖，但滚动动画基本做不了）。

**性能**：重编码 + libass 渲染，必须在 UI 限流：

- 弹幕密度：默认只烧前 N 条（如 500），提供密度滑块
- 时长：建议 ≤ 10 分钟，超出提示
- 开关默认**关闭**（因为慢）

**UI**：面板「合成 MP4」区加 checkbox「烧录弹幕」；勾上后展开密度 / 字号 / 显示区域（顶部 / 全屏 / 底部）选项。

**依赖**：字体验证通过；`libass` 已在 core 中。

---

### 3.2 故事板长图　`[ ]`　工作量：中

**目标**：一张图看清整个视频讲了什么。学习笔记/视频索引场景。

**命令**：

```
-i in.mp4 -vf "fps=1/{interval},scale={width}:-1,tile={cols}x{rows}" -frames:v 1 -y out.jpg
```

- `fps=1/30`：每 30 秒抽一帧
- `tile=4x5`：拼成网格
- `-frames:v 1`：只输出第一屏

**不重编码**，只解码被抽中的帧，比全片编码快一个量级。

**参数**：间隔 10s / 30s / 60s / 自动（按目标张数反算）；列数 3–6，行数按帧数自动 `ceil(帧数/列数)`；缩略图宽度 240 / 320 / 480。

**风险**：`fps` 滤镜是**全解码**再抽帧，1080p 长视频可能要几分钟。UI 上给个耗时预期。另外要限制总帧数，否则 `tile` 只出第一屏、后面的被丢弃（需按帧数动态算行数，或直接限制可生成的张数）。

---

### 3.3 静音自动剪　`[ ]`　工作量：中

**目标**：自动去掉片头等待、中间卡顿的静音段。

**两阶段**：

1. **检测**（解码音频，快）：

   ```
   -i ina.mp4 -af silencedetect=noise=-30dB:d=0.5 -f null -
   ```

   结果输出在 stderr，需在 offscreen 侧捕获并解析出各静音段起止。

2. **裁剪**，三种做法：

   | 做法 | 手段 | 速度 | 说明 |
   |---|---|---|---|
   | **A. 只剪首尾**（推荐先做） | 检测首尾静音 → `-ss` / `-to` → `-c copy` | 秒级 | 覆盖最常见的「切掉片头片尾」需求 |
   | B. 去除中间静音 | `aselect='between(t,a,b)+...',asetpts=N/SR/TB` | 需重编码音频 | 音频编码快，可接受 |
   | C. 分段 copy + concat | 多段 `-c copy` 再拼 | 复杂 | concat 滤镜要重编码，demuxer 方案又很绕，暂不推荐 |

**建议路径**：先做 A（`-c copy` 秒级，性价比最高），验证效果后再补 B。

**注意**：视频要跟着同步裁剪，所以实际操作对象最好是 mux 后的成品 MP4，或对音视频流施加相同的时间区间。

---

### 3.4 loudnorm 音量标准化　`[ ]`　工作量：小

**目标**：B 站各视频音量参差不齐，一开就治好。

**命令**（视频 copy，只重编码音频）：

```
-i in.mp4 -af loudnorm=I=-16:TP=-1.5:LRA=11 -c:v copy -c:a aac -b:a 192k -y out.mp4
```

目标响度可选 -14（YouTube/Spotify 标准）/ -16（推荐默认）/ -18 LUFS。

**单遍 vs 双遍**：

- 单遍：简单，效果已经够用，先做这个
- 双遍（更精确）：先 `-af loudnorm=...:print_format=json -f null -` 测出 `measured_I` 等值，第二遍带回去做线性归一化。代价是音频解码两遍

**代价**：`-c:a aac` 是重编码，AAC → AAC 有代损，但响度统一带来的体验提升远大于这点损失。

**UI**：可作为「合成 MP4」时的勾选项，也可单独按钮。

---

## 4. 第三梯队

### 4.1 波形 / 频谱可视化视频　`[ ]`　工作量：中

**目标**：把音轨渲染成动态可视化视频。音乐区 UP 主可做封面素材、视频片头。

**命令**：

```
-i ina.mp4 -filter_complex "[0:a]showwaves=s=1280x200:mode=line:colors=0xfb7299[v]" -map "[v]" -map 0:a -c:v libx264 -c:a aac -y out.mp4
```

变体：`showspectrum`（彩色频谱瀑布）、`avectorscope`、`ahistogram`。

`showwaves` 的 `mode`：`line` / `p2p` / `cline`。

**两种模式**：

| 模式 | 说明 | 速度 |
|---|---|---|
| **A. 纯可视化**（先做） | 不要原画面，只输出图形 + 音频 | 快，编码量小 |
| B. 原视频 + 波形叠加 | 原画面下方叠一条波形 | 需编码整个视频，慢 |

先做 A。输出 MP4（H.264）或 WebM（VP9）。

---

### 4.2 GIF 表情包工坊　`[ ]`　工作量：中

**目标**：视频片段转高质量 GIF，可加文字。

**两遍编码**（`palettegen` + `paletteuse`，保证色彩不崩）：

```
# 第一遍：生成调色板
-ss {start} -t {duration} -i in.mp4 -vf "fps=12,scale=480:-1:flags=lanczos,palettegen" -y palette.png

# 第二遍：应用调色板
-ss {start} -t {duration} -i in.mp4 -i palette.png \
  -filter_complex "[0:v]fps=12,scale=480:-1:flags=lanczos[x];[x][1:v]paletteuse" -y out.gif
```

**参数**：起止时间；宽度 240 / 320 / 480；帧率 8 / 12 / 15（越高越流畅也越大）。

**加文字**：`drawtext` 滤镜——**注意 drawtext 同样需要 `fontfile`，依赖字体方案**（与 3.1 是同一个前置阻塞）。若字体验证通过，可复用同一份字体。

**必须限流**：≤ 15 秒、≤ 480px 宽。GIF 体积极易爆炸，UI 上要显示预估大小。

---

## 5. 实施顺序建议

```
1. 提取即转码（音频 MP3 / 视频 MP4）        ← 工作量最小，立竿见影
2. 无损片段裁剪                              ← 秒级体验，用户感知强
3. 元数据擦除（作为勾选项并入 1、2）          ← 顺手做，不单独占资源
   ─────────────────────────────────────
4. 【验证关卡】字体 + libass 最小可行性验证    ← 不通过则 3.1 与 4.2 需改道
5. loudnorm 音量标准化                        ← 小工作量，可插队
6. 故事板长图
7. 静音自动剪（先做只剪首尾）
   ─────────────────────────────────────
8. 弹幕烧录（依赖 4 通过）
9. 波形可视化（先做纯可视化模式）
10. GIF 工坊（依赖 4 通过才能加文字）
```

---

## 6. 番剧（PGC）下载方案

### 6.1 为什么番剧是「另一条链路」

番剧/影视（`/bangumi/play/ss*` 与 `/bangumi/play/ep*`）跟普通视频走的完全是两套接口，三处最容易踩空：

| | UGC（普通视频） | PGC（番剧） |
|---|---|---|
| 元信息 | `/x/web-interface/view`，成功体是 **`data`** | `/pgc/view/web/season`，成功体是 **`result`** |
| 取流 | `/x/player/wbi/playurl`（强制 WBI 签名） | `/pgc/player/web/v2/playurl`（签名要求反复变动，见下） |
| DASH 位置 | `result.dash` | **`result.video_info.dash`**（v2 把老的 `result` 整体塞进了 `video_info`） |
| 剧集关系 | 分 P：`view.pages[]`，`?p=` 切 | 剧集：`episodes[]` / `sections[]` / `modules[].data`，**靠 path 里的 ss/ep 切换** |
| 会员限制 | 基本没有 | `code = -10403`，或 `play_check.play_detail = PLAY_PREVIEW`（只给试看片段） |
| FLV 分段 | 多数可用 | **绝大多数没有**，所以「兼容下载」按钮在番剧页要整行隐藏 |

因为存在这些差异，代码里把两条链路**归一化成同一个结构**（`{ dash: { video, audio } }`）后再交给面板，下载/合成逻辑一行都不用改。

### 6.2 已实现的范围（已实测通过：按钮出现、节点 3 个、会员文案正确、下载成功）

- URL 识别：`/bangumi/play/ss109700` → `season_id=109700`；`/bangumi/play/ep321808` → `ep_id=321808`
- 按钮注入：番剧页工具栏容器与视频页不同名，选择器加了 `.toolbar-left` 等；**所有选择器都不命中时退化为右上角悬浮按钮**，保证入口一定存在
- 剧集列表：同时收 `episodes` / `sections[].episodes` / `modules[].data.episodes`（含 `modules[].data.sections[]`），按 `ep_id` 去重——只认一种会表现为「选集是空的」
- 面板新增「选集」下拉，切一集重新取一次该集的 playurl
- `ss` 入口没有指定哪一集，默认第一集；`ep` 入口精确定位
- 会员/试看/风控错误码翻译成人话（`-10403` 大会员、`-412` 风控、`PLAY_PREVIEW` 试看提示）
- 兼容性防御：部分剧集在 season 接口里不带 `cid`，退回按 `bvid`（课程按 `aid`）走 view 接口补

**第二轮优化（2026-09-08 同日）**

| 项 | 说明 |
|---|---|
| 按钮标签 `span` 而非 `div` | 番剧页工具栏是 inline-flex，块级 div 会被撑成整行、破坏点赞/投币/收藏的排列 |
| 清晰度默认不再是 HDR | 见下方 6.4 |
| 课程 `/cheese/*` 支持 | 见下方 6.5 |

### 6.4 清晰度默认值为什么会落在 125（HDR）

清晰度编号 125 = **HDR**，126 = 杜比视界，都是「色域/传输函数不同」的一档：

- 拷进普通 MP4 后在 SDR 屏上会**明显偏色发灰**，用户会以为下载器坏了
- 之前默认落在 125，原因有两个：① `fnval=4048` 会把 HDR 档一起返回（UGC 用的 `fnval=16` 不会）；② 下拉按接口返回顺序排、`list[0]` 就是默认

现在的处理：

```
QN_NOT_DEFAULT = [125, 126, 127]   // HDR / 杜比视界 / 8K
```

- 这三档**仍然列在下拉里**供手动选择，只是不作为默认项
- 选项文案带提示：`HDR · 普通屏偏色 (2650kbps)`
- 默认取「最高的、普通设备能正常看的」那一档（通常落到 116 或 80）
- `QN_LABEL` 补齐了 125 / 126 / 6 三档（之前缺失导致下拉显示为裸数字 `125`）

### 6.5 课程（PUGV）`/cheese/*`

与番剧同族，但**三处不一样**，照搬番剧代码必然失败：

| | 番剧 PGC | 课程 PUGV |
|---|---|---|
| 元信息 | `/pgc/view/web/season` → **`result`** | `/pugv/view/web/season` → **`data`** |
| 取流 | `/pgc/player/web/v2/playurl`，给 `ep_id` 即可 | `/pugv/player/web/playurl`，**必须显式带 `avid` + `cid`**，只给 `ep_id` 报参数错误 |
| DASH 位置 | `result.video_info.dash` | `data.dash`（**没有 video_info 这一层**） |

其他差异：

- 面板下拉在课程页显示为「课程目录」而非「选集」
- 课程没有 `bvid`，只有 `aid`，cid 缺失时的兜底走 `av{AID}`
- 课程 `episodes[].title` 是「1. 课程介绍」这类小节名，不是纯数字 → 不能拼成「第X集」（番剧才是数字集数）
- `-10403` 在课程场景=**没买课**，不是大会员；错误文案要分开
- 付费课程同理：只能拿到当前账号已购买的内容

### 6.3 还没做、需要考虑的

**① 整季批量下载（最大呼声，但别急着做）**

关键：**不存在「一次拿全季直链」的接口**。每集的直链按各自的 cid 签发，必须逐集请求 playurl。

难点在于中间状态的处理，而不是「发 N 个请求」本身：

- 会员权限每集不同（常见的「前 2 集免费、其余大会员」），批量跑到第 3 集就会断，需要「跳过并汇总失败项」而不是整体失败
- 直链有效期 120 分钟，整季 24 集串行下载容易超时，需要**边下边取**（下第 N 集前才拿第 N 集的链），而不是先一口气把 24 条链接囤着
- 浏览器内存：现有链路是「整集拉进 ArrayBuffer → 分块传给 offscreen」。串行 + 每集完成后及时释放，理论可行，但**没有并发额度**，做不了多线程加速；建议先限制「最多同时排 N 集 = 1」
- 落地文件名要防重复（`第01集` 补零）

建议形态：**先做「勾选多集 → 串行队列」**，不要做并发下载。

**② 区域/版权限制**

- 部分番剧仅中国大陆可看，`/pgc/view/web/season` 会直接失败或返回空列表
- 页面上会渲染 `AreaLimitPanel`，API 侧没有明确错误码，只能靠「列表为空」兜底 → 目前是提示"可能需登录或该地区不可观看"
- 这类情况没有通用解法，属版权方限制

**③ 会员权益的现实边界**

- 技术上能下的是「当前账号能播的那一版」。不是大会员就只有 480P/试看，这是服务端发的直链决定的，跟下载器无关
- 「为什么不给我 1080P」这类反馈，标准答案是：**先在播放器里确认能不能看 1080P**，播放器播不了的下载器一定拿不到

**④ 多季（第二季、第三季）**

`season` 结果里有 `seasons[]`，可做「整系列」导航。优先级低，先放一放。

**⑤ 同类页面覆盖**

- **课程 `/cheese/*`：已完成**（见 6.5）
- **影视 / 纪录片 / 综艺**：已经在番剧链内（`type` 由 season 接口返回，不影响取流）
- 再往后若要扩站点（YouTube 等）：此时已有 UGC / PGC / PUGV 三个适配器，新增只需补一套「URL 识别 + 元信息 + 取流并归一化成 `{ dash: { video, audio } }`」，UI 与下载逻辑不用动

---

## 7. 进度追踪

| 功能 | 梯队 | 状态 | 工作量 | 阻塞项 |
|---|---|---|---|---|
| 番剧单集下载（含选集） | 一 | `[x]` | 中 | — |
| 课程 /cheese 支持 | 一 | `[x]` | 小 | — |
| 清晰度默认值避开 HDR | 一 | `[x]` | 小 | — |
| 番剧整季批量下载 | 二 | `[ ]` | 中 | 串行队列 + 失败跳过策略 |
| 提取音频 → MP3 | 一 | `[ ]` | 小 | — |
| 提取视频 → MP4 | 一 | `[ ]` | 小 | — |
| 无损片段裁剪 | 一 | `[ ]` | 中 | — |
| 元数据擦除 | 一 | `[ ]` | 小 | 建议并入其他功能 |
| 弹幕烧录（开关） | 二 | `[ ]` | 大 | **字体 + libass 验证** |
| 故事板长图 | 二 | `[ ]` | 中 | — |
| 静音自动剪 | 二 | `[ ]` | 中 | — |
| loudnorm 音量标准化 | 二 | `[ ]` | 小 | — |
| 波形 / 频谱可视化 | 三 | `[ ]` | 中 | — |
| GIF 表情包工坊 | 三 | `[ ]` | 中 | 加文字需字体方案 |

---

## 8. 与既有工作并行的事项

- **CDN 节点容错**（已改完待实测）：`fetchStreamWithFallback` / `downloadStreamWithFallback`，兼容 `backupUrl` 与 `backup_url` 两套字段名。修复的是「主节点一挂整次下载就失败」的设计缺陷
- **版本号**：当前 v1.2.3（2026-09-12：修复商店「远程托管代码」拒审 + 收紧 web_accessible_resources）；v1.2.1（2026-09-09：工具栏图标更新 + 文案改写）；v1.2.0（2026-09-08）含番剧/课程支持、CDN 容错、UI 与文件名优化
